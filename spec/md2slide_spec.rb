# frozen_string_literal: true

RSpec.describe Md2slide do
  it "has a version number" do
    expect(Md2slide::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
